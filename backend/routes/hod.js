/*import express from 'express';
import { verifyToken } from '../middleware/authMiddleware.js';
import { getFeedbackSummaryForHOD } from '../controllers/hodController.js';

const router = express.Router();

// Route for HOD to get feedback summary
router.get('/feedback-summary', verifyToken, getFeedbackSummaryForHOD);

export default router;*/
